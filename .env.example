# ──────────────────────────────────────────────
# Copia este archivo como .env y completa los valores
# cp .env.example .env
# ──────────────────────────────────────────────

# Base de datos MySQL
MYSQL_ROOT_PASSWORD=cambia-esta-password-root
MYSQL_DATABASE=peluqueria
MYSQL_USER=stratus
MYSQL_PASSWORD=cambia-esta-password-stratus

# Backend
# Clave para FIRMAR los JWT de login. OBLIGATORIA en staging/prod.
# Si falta y PRODUCTION_DOMAIN está seteado, el backend se niega a arrancar.
# Debe ser distinta por entorno. Cambiarla invalida todas las sesiones activas.
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=clave-larga-y-aleatoria-para-jwt

# Clave secreta para el endpoint /auth/superadmin/setup (úsala una sola vez)
SUPERADMIN_SETUP_SECRET=clave-secreta-larga-y-aleatoria

# Clave para cifrar las credenciales de Mercado Pago de cada salón.
# IMPORTANTE: una vez en uso NO se debe cambiar (rompería los tokens guardados).
# Cada entorno (staging/prod) puede tener su propia clave (son bases distintas).
# Generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MP_ENCRYPTION_KEY=clave-fernet-generada

# Dominio de producción (sin https://, sin www)
# Permite CORS para tudominio.com y *.tudominio.com
PRODUCTION_DOMAIN=tudominio.com

# URL pública HTTPS del backend (con la que Mercado Pago alcanza el webhook).
# Importante en staging/prod detrás de proxy. Sin barra final.
# Ej: https://api-staging.tudominio.com
BACKEND_PUBLIC_URL=https://api.tudominio.com

# Orígenes extra para CORS separados por coma (frontends de staging que no sean
# subdominio de PRODUCTION_DOMAIN). Ej: https://bluemoon-staging.tudominio.com
EXTRA_ORIGINS=

# URL pública de la API — se usa en el BUILD de los tres frontends
# Debe ser la URL con la que el navegador del cliente llega al backend
VITE_API_URL=https://api.tudominio.com
